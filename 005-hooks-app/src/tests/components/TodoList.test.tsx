
describe('Test in TodoList', () => {
  // const toggleTodo = jest.fn()
  // const deleteTodo = jest.fn()
  // const wrapper = shallow(<TodoList toggleTodo={toggleTodo} deleteTodo={deleteTodo} todos={demoTodos}  />)
  test('snapshot', () => {
    // expect(wrapper).toMatchSnapshot()
  })
  test('Must have two TodoItem', () => {
    // expect(wrapper.find("TodoItem").length).toBe(demoTodos.length)
    // expect(wrapper.find("TodoItem").at(0).prop("handleDelete")).toEqual(expect.any(Function))
  })
})